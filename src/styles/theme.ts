import { DefaultTheme } from 'styled-components/dist/types';
import { TCLightPalette } from './colors';
import { TCFont } from './fonts';

export const TCtheme: DefaultTheme = {
  colors: TCLightPalette,
  fonts: TCFont,
};
